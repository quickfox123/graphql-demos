# import Agent from "domain/agent.graphql"
# import Client from "domain/client.graphql"
# import Policy from "domain/policy.graphql"

type Query {
  agentData:[Agent] 
  clientData:[Client]
  getAgentById(taxId: Int): Agent
  getClientById(hcid: Int): Client
}